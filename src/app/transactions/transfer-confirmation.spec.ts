import { TransferConfirmation } from './transfer-confirmation';

describe('TransferConfirmation', () => {
  it('should create an instance', () => {
    expect(new TransferConfirmation()).toBeTruthy();
  });
});
